{% extends 'base.html.twig' %}

{% block title %}Exo table auteur
{% endblock %}

{% block body %}
	<style>
		table,
		td {
			border: 1px solid #333;
		}

		thead,
		tfoot {
			background-color: #333;
			color: #fff;
		}
	</style>
    {#{ dump(livres) }#}

    <h1>Bienvenue à la bliblio</h1>

	<table>
		<thead>
			<tr>
				<th colspan="3">la table des livre</th>
			</tr>
			<tr>
				<th>id</th>
				<th>titre</th>
				<th>action</th>
			</tr>
		</thead>
        <tbody>
			{% for livre in livres %}
				<tr>
					<td>{{ livre.id }}</td>
					<td>{{ livre.titre }}</td>
                    <td>
                        <a href="{{ path('app_livre_fiche', {id: livre.id}) }}">voir</a>
						<a href="{{ path('app_livre_modifier', {id: livre.id}) }}">modifier</a>
                    </td>
				</tr>
			{% endfor %}
		</tbody>
	</table>
{% endblock %}
